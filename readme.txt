Need node.js and websocket-server npm

Installing pre-requisite On MacOSX :
brew install node
curl http://npmjs.org/install.sh | sh
export NODE_PATH=
npm install websocket-server

Running :

node ws.js
open clock.html clock.html clock.html
